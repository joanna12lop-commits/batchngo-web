import type{ReactNode}from"react";import{publicMetadata}from"../../lib/seo";
export const metadata=publicMetadata("Find Small-Batch Manufacturers","Explore curated example profiles and filter small-batch manufacturing capabilities by category, location and production needs.","/find-makers");
export default function Layout({children}:{children:ReactNode}){return children}
